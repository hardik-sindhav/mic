# Backend Deployment Templates
# This file is for reference only - DO NOT use as a workflow
# Copy templates to deploy.yml instead

# NOTE: This file should NOT execute - keeping only for reference
on:
  workflow_dispatch: # Manual trigger only

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is a template file. Use templates in deploy.yml instead."

# ================================================================================
# REFERENCE TEMPLATES BELOW - Copy to deploy.yml as needed
# ================================================================================

# ============================================
# TEMPLATE 1: Render Deployment
# ============================================
# backend-deploy-render:
#   runs-on: ubuntu-latest
#   
#   steps:
#   - uses: actions/checkout@v4
#   
#   - name: Deploy to Render
#     run: |
#       curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}
#   
#   - name: Wait for deployment
#     run: sleep 10
#   
#   - name: Verify deployment
#     run: |
#       curl -f https://${{ secrets.RENDER_SERVICE_URL }}/health || exit 1

# ============================================
# TEMPLATE 2: Railway Deployment
# ============================================
# backend-deploy-railway:
#   runs-on: ubuntu-latest
#   
#   steps:
#   - uses: actions/checkout@v4
#   
#   - name: Setup Node.js
#     uses: actions/setup-node@v4
#     with:
#       node-version: '20.x'
#   
#   - name: Install Railway CLI
#     run: npm install -g @railway/cli
#   
#   - name: Deploy to Railway
#     env:
#       RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
#     run: |
#       railway link ${{ secrets.RAILWAY_PROJECT_ID }}
#       railway deploy
#   
#   - name: Verify deployment
#     run: |
#       sleep 5
#       curl -f https://${{ secrets.RAILWAY_DOMAIN }}/health || exit 1

# ============================================
# TEMPLATE 3: Heroku Deployment
# ============================================
# backend-deploy-heroku:
#   runs-on: ubuntu-latest
#   
#   steps:
#   - uses: actions/checkout@v4
#   
#   - name: Heroku Deployment
#     uses: akhileshns/heroku-deploy@v3.13.15
#     with:
#       heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
#       heroku_app_name: ${{ secrets.HEROKU_APP_NAME }}
#       heroku_email: ${{ secrets.HEROKU_EMAIL }}
#       appdir: backend

# ============================================
# TEMPLATE 4: Docker + ECR + AWS ECS
# ============================================
# backend-deploy-aws:
#   runs-on: ubuntu-latest
#   
#   steps:
#   - uses: actions/checkout@v4
#   
#   - name: Configure AWS credentials
#     uses: aws-actions/configure-aws-credentials@v4
#     with:
#       aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#       aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#       aws-region: us-east-1
#   
#   - name: Login to Amazon ECR
#     id: login-ecr
#     uses: aws-actions/amazon-ecr-login@v2
#   
#   - name: Build, tag, and push image to Amazon ECR
#     env:
#       ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#       ECR_REPOSITORY: mic-backend
#       IMAGE_TAG: latest
#     run: |
#       docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG backend/
#       docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
#   
#   - name: Update ECS service
#     run: |
#       aws ecs update-service \
#         --cluster ${{ secrets.ECS_CLUSTER }} \
#         --service ${{ secrets.ECS_SERVICE }} \
#         --force-new-deployment

# ============================================
# TEMPLATE 5: SSH Deploy to VPS
# ============================================
# backend-deploy-vps:
#   runs-on: ubuntu-latest
#   
#   steps:
#   - uses: actions/checkout@v4
#   
#   - name: Deploy to VPS
#     env:
#       DEPLOY_KEY: ${{ secrets.VPS_DEPLOY_KEY }}
#       VPS_USER: ${{ secrets.VPS_USER }}
#       VPS_HOST: ${{ secrets.VPS_HOST }}
#       APP_PATH: /home/app/backend
#     run: |
#       mkdir -p ~/.ssh
#       echo "$DEPLOY_KEY" > ~/.ssh/deploy_key
#       chmod 600 ~/.ssh/deploy_key
#       ssh-keyscan -H $VPS_HOST >> ~/.ssh/known_hosts
#       ssh -i ~/.ssh/deploy_key $VPS_USER@$VPS_HOST << 'EOF'
#         cd $APP_PATH
#         git pull origin main
#         npm ci
#         npm run build 2>/dev/null || true
#         sudo systemctl restart mic-backend
#       EOF

# ============================================
# TEMPLATE 6: DigitalOcean App Platform
# ============================================
# backend-deploy-digitalocean:
#   runs-on: ubuntu-latest
#   
#   steps:
#   - uses: actions/checkout@v4
#   
#   - name: Deploy to DigitalOcean
#     uses: digitalocean/app_action@main
#     with:
#       app_name: mic-backend
#       token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

# ============================================
# TEMPLATE 7: Vercel (for if you use Next.js)
# ============================================
# backend-deploy-vercel:
#   runs-on: ubuntu-latest
#   
#   steps:
#   - uses: actions/checkout@v4
#   
#   - name: Deploy with Vercel
#     uses: amondnet/vercel-action@v25
#     with:
#       vercel-token: ${{ secrets.VERCEL_TOKEN }}
#       vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
#       vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
#       working-directory: backend

# ============================================
# TEMPLATE 8: GitHub Container Registry Deploy
# ============================================
# backend-deploy-ghcr:
#   runs-on: ubuntu-latest
#   
#   permissions:
#     contents: read
#     packages: write
#   
#   steps:
#   - uses: actions/checkout@v4
#   
#   - name: Set up Docker Buildx
#     uses: docker/setup-buildx-action@v3
#   
#   - name: Log in to Container Registry
#     uses: docker/login-action@v3
#     with:
#       registry: ghcr.io
#       username: ${{ github.actor }}
#       password: ${{ secrets.GITHUB_TOKEN }}
#   
#   - name: Build and push Docker image
#     uses: docker/build-push-action@v5
#     with:
#       context: backend/
#       push: true
#       tags: |
#         ghcr.io/${{ github.repository }}/backend:latest
#         ghcr.io/${{ github.repository }}/backend:${{ github.sha }}

# ============================================
# Instructions to use:
# ============================================
# 1. Choose the template that matches your host provider
# 2. Uncomment the entire job section
# 3. Copy it to .github/workflows/deploy.yml
# 4. Replace placeholders with your actual values
# 5. Add required secrets to GitHub (Settings → Secrets)
# 6. Commit and test
# ============================================
